# Runtime Store 模块

本模块用于承载当前成品阶段的存储抽象与仓储实现，负责：

- 管理项目元数据
- 管理项目目录内容
- 管理编译任务状态
- 管理正式认证、组织团队、审计日志和版本事件
- 管理 PostgreSQL 结构化平台 schema 和对象存储适配入口

后续如果切换到 PostgreSQL、Redis、对象存储，应优先替换本模块，而不是让上层服务直接感知底层存储变化。
